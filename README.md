# aws
aws solution arch
vpc(virtual private cloud)=
   subnet
   route table 
   internet gateway
   NAT gateway
   egress only on internet gateway
   DHCP options set
   elastic IP
   managed prefix lists
   end points
   peer connections
